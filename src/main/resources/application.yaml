spring:
  application:
    name: loan-approval
  data:
    mongodb:
      uri: mongodb://loan-approval:loan-approval@localhost:27017/loan-approval
workerId: blueprint
loan-approval:
  max-amount: 10000
